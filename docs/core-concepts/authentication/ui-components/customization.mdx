---
title: Customization
description: Customize authentication components with hierarchical appearance system
---

The Insforge UI packages provide a powerful **hierarchical appearance system** that allows you to customize every aspect of authentication components—from top-level containers to nested elements like form fields and buttons.

<Note>
**Framework Support**: This customization system works identically across all Insforge UI packages:
- `@insforge/nextjs` (Next.js)
- `@insforge/react` (Vite + React Router)
</Note>

## Hierarchical Appearance System

The hierarchical appearance system lets you style nested components through a JSON-like structure, giving you complete control over every element.

**Example:**
```tsx
<SignIn
  appearance={{
    card: "bg-white shadow-2xl",
    header: {
      title: "text-3xl font-bold",
      subtitle: "text-gray-600"
    },
    form: {
      emailField: {
        input: "border-blue-500"
      }
    },
    button: "bg-blue-600"
  }}
/>
```

## Complete Appearance Structure

### SignIn / SignUp Components

Full appearance hierarchy for authentication forms:

```tsx
import { SignIn } from '@insforge/nextjs'; // or '@insforge/react'

<SignIn
  appearance={{
    container?: string;              // Outermost wrapper
    card?: string;                   // Inner card box
    
    header?: {
      container?: string;            // Header wrapper
      title?: string;                // Title text
      subtitle?: string;             // Subtitle text
    };
    
    errorBanner?: string;            // Error message banner
    
    form?: {
      container?: string;            // Form wrapper
      
      emailField?: {
        container?: string;          // Email field wrapper
        label?: string;              // Email label
        input?: string;              // Email input
      };
      
      passwordField?: {
        container?: string;          // Password field wrapper
        label?: string;              // Password label
        input?: string;              // Password input
        forgotPasswordLink?: string; // Forgot password link (SignIn only)
        
        strengthIndicator?: {        // Password strength (SignUp only)
          container?: string;
          requirement?: string;
        };
      };
    };
    
    button?: string;                 // Submit button
    
    link?: {
      container?: string;            // Link section wrapper
      text?: string;                 // Link description text
      link?: string;                 // Actual link element
    };
    
    divider?: string;                // "or" divider
    
    oauth?: {
      container?: string;            // OAuth buttons wrapper
      button?: string;               // Individual OAuth button
    };
  }}
/>
```

### UserButton Component

```tsx
<UserButton
  appearance={{
    button?: string;        // Main button
    name?: string;          // User name text
    email?: string;         // User email text
    dropdown?: string;      // Dropdown menu
  }}
/>
```

## Real-World Examples

### Example 1: Modern Purple Theme

```tsx
<SignIn
  appearance={{
    card: "bg-gradient-to-br from-purple-50 to-white shadow-2xl",
    
    header: {
      title: "text-3xl font-bold text-purple-900",
      subtitle: "text-purple-600 mt-2"
    },
    
    form: {
      emailField: {
        label: "text-gray-800 font-semibold text-sm uppercase tracking-wide",
        input: "border-2 border-purple-200 focus:border-purple-500 focus:ring-2 focus:ring-purple-200 rounded-xl"
      },
      
      passwordField: {
        label: "text-gray-800 font-semibold text-sm uppercase tracking-wide",
        input: "border-2 border-purple-200 focus:border-purple-500 focus:ring-2 focus:ring-purple-200 rounded-xl",
        forgotPasswordLink: "text-purple-600 hover:text-purple-700 font-medium"
      }
    },
    
    button: "bg-gradient-to-r from-purple-600 to-purple-700 hover:from-purple-700 hover:to-purple-800 text-white font-bold py-4 rounded-xl shadow-lg hover:shadow-xl transition-all",
    
    link: {
      text: "text-gray-600",
      link: "text-purple-600 hover:text-purple-700 font-semibold underline"
    },
    
    oauth: {
      button: "border-2 border-purple-200 hover:border-purple-300 hover:bg-purple-50 transition-colors rounded-xl"
    }
  }}
/>
```

### Example 2: Minimal Dark Theme

```tsx
<SignIn
  appearance={{
    card: "bg-gray-900 shadow-2xl border border-gray-800",
    
    header: {
      title: "text-2xl font-semibold text-white",
      subtitle: "text-gray-400"
    },
    
    form: {
      emailField: {
        label: "text-gray-300 text-sm",
        input: "bg-gray-800 border-gray-700 text-white focus:border-blue-500 rounded-lg"
      },
      
      passwordField: {
        label: "text-gray-300 text-sm",
        input: "bg-gray-800 border-gray-700 text-white focus:border-blue-500 rounded-lg",
        forgotPasswordLink: "text-blue-400 hover:text-blue-300"
      }
    },
    
    button: "bg-blue-600 hover:bg-blue-700 text-white font-medium py-3 rounded-lg",
    
    link: {
      text: "text-gray-400",
      link: "text-blue-400 hover:text-blue-300"
    },
    
    oauth: {
      button: "bg-gray-800 border-gray-700 hover:bg-gray-750 text-white"
    }
  }}
/>
```

### Example 3: Clean Corporate Style

```tsx
<SignIn
  appearance={{
    card: "bg-white shadow-sm border-2 border-gray-200",
    
    header: {
      title: "text-2xl font-bold text-gray-900",
      subtitle: "text-gray-600"
    },
    
    form: {
      emailField: {
        label: "text-gray-700 font-medium",
        input: "border-gray-300 focus:border-blue-600 focus:ring-1 focus:ring-blue-600"
      },
      
      passwordField: {
        label: "text-gray-700 font-medium",
        input: "border-gray-300 focus:border-blue-600 focus:ring-1 focus:ring-blue-600"
      }
    },
    
    button: "bg-blue-600 hover:bg-blue-700 font-semibold",
    
    oauth: {
      button: "border-gray-300 hover:bg-gray-50"
    }
  }}
/>
```

## Text Customization

All components support complete text customization:

### SignIn Component

```tsx
<SignIn
  title="Welcome Back!"
  subtitle="We're happy to see you again"
  emailLabel="Your Email Address"
  emailPlaceholder="you@company.com"
  passwordLabel="Your Password"
  passwordPlaceholder="Enter your password"
  submitButtonText="Login Now"
  loadingButtonText="Signing you in..."
  forgotPasswordText="Forgot password?"
  signUpText="New to our platform?"
  signUpLinkText="Create an account"
  dividerText="or continue with"
/>
```

### SignUp Component

```tsx
<SignUp
  title="Create Account"
  subtitle="Get started with your free account"
  emailLabel="Email Address"
  passwordLabel="Choose Password"
  submitButtonText="Create Account"
  loadingButtonText="Creating your account..."
  signInText="Already have an account?"
  signInLinkText="Sign in"
  dividerText="or sign up with"
/>
```

### UserButton Component

```tsx
<UserButton
  mode="detailed"  // or "simple"
  afterSignOutUrl="/"
  appearance={{
    button: "hover:bg-gray-100 rounded-full p-2",
    name: "text-gray-900 font-semibold",
    email: "text-gray-500 text-sm",
    dropdown: "shadow-xl border border-gray-200"
  }}
/>
```

**Display Modes:**
- `detailed` (default): Shows avatar + name + email
- `simple`: Shows avatar only

## Styling Atomic Components

When building custom forms with atomic components, each supports its own appearance structure:

### AuthContainer

```tsx
<AuthContainer
  appearance={{
    container: "max-w-md mx-auto",
    card: "bg-white shadow-2xl rounded-2xl p-8"
  }}
>
  {/* Your form content */}
</AuthContainer>
```

### AuthHeader

```tsx
<AuthHeader
  title="Welcome Back"
  subtitle="Sign in to continue"
  appearance={{
    container: "mb-8 text-center",
    title: "text-3xl font-bold text-gray-900",
    subtitle: "text-gray-600 mt-2"
  }}
/>
```

### AuthFormField

```tsx
<AuthFormField
  id="email"
  type="email"
  label="Email Address"
  placeholder="you@example.com"
  appearance={{
    container: "mb-4",
    label: "text-gray-700 font-semibold mb-2",
    input: "border-2 border-gray-300 focus:border-blue-500 rounded-lg px-4 py-3"
  }}
/>
```

### AuthPasswordField

```tsx
<AuthPasswordField
  id="password"
  label="Password"
  emailAuthConfig={config}
  showStrengthIndicator
  forgotPasswordLink={{
    href: "/forgot-password",
    text: "Forgot password?"
  }}
  appearance={{
    container: "mb-4",
    label: "text-gray-700 font-semibold mb-2",
    input: "border-2 border-gray-300 focus:border-blue-500 rounded-lg px-4 py-3"
  }}
/>
```

### AuthSubmitButton

```tsx
<AuthSubmitButton
  isLoading={loading}
  confirmed={success}
  className="w-full bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 rounded-lg"
>
  Sign In
</AuthSubmitButton>
```

### AuthOAuthProviders

```tsx
<AuthOAuthProviders
  providers={['google', 'github', 'discord']}
  onClick={handleOAuth}
  loading={oauthLoading}
  appearance={{
    container: "space-y-3",
    button: "border-2 border-gray-200 hover:bg-gray-50 rounded-lg"
  }}
/>
```

**Adaptive Layout:**
- 1 provider: Full width with full text ("Sign in with Google")
- 2 providers: Two columns with short text ("Google", "GitHub")
- 3+ providers: Three columns with icons only

### AuthDivider

```tsx
<AuthDivider 
  text="or continue with"
  className="my-6 text-gray-500"
/>
```

### AuthLink

```tsx
<AuthLink
  text="Don't have an account?"
  linkText="Sign up"
  href="/sign-up"
  appearance={{
    container: "mt-6 text-center",
    text: "text-gray-600",
    link: "text-blue-600 hover:underline font-semibold ml-1"
  }}
/>
```

## Best Practices

<CardGroup cols={2}>
  <Card title="Start Simple" icon="lightbulb">
    Begin with default styling, then customize only what you need
  </Card>
  
  <Card title="Use Tailwind" icon="paintbrush">
    All appearance props accept Tailwind CSS classes for consistency
  </Card>
  
  <Card title="Test Responsively" icon="mobile">
    Test your customizations on mobile, tablet, and desktop
  </Card>
  
  <Card title="Maintain Contrast" icon="eye">
    Ensure text remains readable on custom background colors
  </Card>
</CardGroup>

<Tip>
**Pro Tip**: Use your app's existing Tailwind design tokens (colors, spacing, fonts) in appearance props to maintain visual consistency.
</Tip>

## Framework-Specific Notes

### Next.js

```tsx
// app/sign-in/page.tsx
'use client';

import { SignIn } from '@insforge/nextjs';

export default function SignInPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-50">
      <SignIn
        appearance={{
          // Your customization here
        }}
      />
    </div>
  );
}
```

### React (Vite, React Router)

```tsx
// src/pages/SignInPage.tsx
import { SignIn } from '@insforge/react';

export default function SignInPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-50">
      <SignIn
        appearance={{
          // Your customization here
        }}
      />
    </div>
  );
}
```

<Note>
The appearance API is identical across all frameworks. Styles defined in one framework will work in another without modification.
</Note>

## TypeScript Support

Full TypeScript support with exported appearance types:

```tsx
import type {
  SignInAppearance,
  SignUpAppearance,
  ForgotPasswordAppearance,
  ResetPasswordAppearance,
  UserButtonAppearance,
} from '@insforge/nextjs'; // or '@insforge/react'

const myCustomAppearance: SignInAppearance = {
  card: "bg-white shadow-xl",
  header: {
    title: "text-3xl font-bold",
    subtitle: "text-gray-600"
  },
  // TypeScript ensures all keys are valid
};

<SignIn appearance={myCustomAppearance} />
```

## Utility Functions

### cn() - Class Merging Utility

Merge Tailwind classes intelligently (combines `clsx` and `tailwind-merge`):

```tsx
import { cn } from '@insforge/nextjs'; // or '@insforge/react'

const className = cn(
  'px-4 py-2',
  'bg-blue-500',
  isActive && 'bg-blue-700',
  'hover:bg-blue-600'
);
// Resolves conflicting classes intelligently
```

## Resources

<CardGroup cols={3}>
  <Card title="Next.js Package" icon="code" href="/core-concepts/authentication/ui-components/nextjs">
    Next.js specific documentation
  </Card>
  
  <Card title="React Package" icon="react" href="/core-concepts/authentication/ui-components/react">
    React specific documentation
  </Card>
  
  <Card title="Tailwind CSS" icon="paintbrush" href="https://tailwindcss.com/docs">
    Tailwind CSS documentation
  </Card>
</CardGroup>

