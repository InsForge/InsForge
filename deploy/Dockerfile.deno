FROM denoland/deno:alpine-2.0.6

# The denoland/deno base image already ships with a 'deno' user and group,
# so no addgroup/adduser is needed here.

WORKDIR /app/functions

COPY --chown=deno:deno functions /app/functions

USER deno
